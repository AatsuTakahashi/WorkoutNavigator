import { StyleSheet } from 'react-native';

export const WelcomeMessageStyle = StyleSheet.create({
  container: {
    padding: 10,
    margin: 10,
  },
  messageText: {
    fontSize: 50,
    fontWeight: 'bold',
  },
});
